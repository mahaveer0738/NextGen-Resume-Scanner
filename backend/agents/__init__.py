"""
============================================================
  agents/__init__.py  —  Makes 'agents' a Python Package

  📌 WHAT IS __init__.py?
     In Python, a folder becomes a "package" (importable
     module) only if it has an __init__.py file inside it.
     It can be empty — just its presence is enough.

     Without this file:
       from agents.ats_agent import run_ats_agent   ← ERROR
     With this file:
       from agents.ats_agent import run_ats_agent   ← WORKS ✅
============================================================
"""

# This file intentionally left mostly empty.
# It just signals to Python: "Hey, this folder is a package!"
